<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.waewaee.waeebasic">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.WaeeBasic">
        <activity android:name=".homework.PetDetailActivity"></activity>
        <activity android:name=".homework.PetActivity">

        </activity>
        <activity android:name=".picker.DatePickerDialogActivity"></activity>
        <activity android:name=".picker.DatePickerActivity" />
        <activity android:name=".ui.DialogActivity" />
        <activity android:name=".ui.UIActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".SecondActivity" />
    </application>

</manifest>